server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;

  # SPA calls the API on yogastudio-s.airepro.in directly (VITE_API_BASE_URL).
  location / {
    try_files $uri $uri/ /index.html;
  }
}
